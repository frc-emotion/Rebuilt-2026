# Rebuilt-2026 Codebase Map

Architecture and style law lives in `.claude/skills/frc-java-architecture/`. Read that skill before touching code. This file is only the map of THIS codebase: facts and gotchas a developer needs that are specific to this robot.

## Growth rule for this file

An entry earns a place here only if BOTH are true:
1. It is specific to this codebase, not general Java/WPILib knowledge.
2. Getting it wrong wasted real time at least once, or would clearly have.

Entries are one or two lines each. When an entry turns out to be a general principle, promote it to the skill's reference files and remove it from here. Prune anything stale. Target length: under two pages, forever.

## Codebase facts

**Layout (skill-server architecture):** `frc.robot.runtime/` is the whole robot-level system. Actuator hardware is DATA in `src/main/deploy/mechanisms.json`, read by the generic `Mechanisms` layer (one `RealMechanism`/`SimMechanism` per motor — replaces the six old IO triads). The decision system is `src/main/deploy/skills.json` (declarative skill table) + `SkillInterpreter` (stateless, drives concurrent intake + scoring axes). Real-time reflexes live in `runtime/reflex/` (`ScoringSequencer` is the pure transition logic — change it and `ScoringSequencerTest` + `docs/skills.md` in the same PR). Computed setpoints are `Setpoints` (interp tables) + `TurretAiming`. The NT coprocessor seam is `SkillServer`; standalone operation is `LocalSkillDriver`. Only `subsystems/drive/` (frozen CTRE) and `subsystems/vision/` (kept behind `runtime.perception.PerceptionProvider`) remain under `subsystems/`. `frc.robot.legacy` is the pre-refactor archive: EXCLUDED from compilation via build.gradle sourceSets — never import it. (The superstructure-era `superstructure/` package and per-mechanism IO triads were DELETED at the skill-server cutover; git + the `refactor/superstructure` branch preserve them.)

- **Frozen generated files:** `subsystems/drive/TunerConstants.java` and `CommandSwerveDrivetrain.java` are stock Phoenix Tuner X output — never edit (Spotless excludes them; verify byte-identical with git diff). Everything else talks to `Drive`, the thin adapter.
- **`mechanisms.json` + `skills.json` ship via the deploy static-files artifact and are schema-validated at load with a SAFE FALLBACK** — a missing/malformed file loads a safe default (mechanisms idle, safe-idle skill, robot disabled-safe) and reports a DriverStation error; it never crashes or runs a half-parsed table. Gear ratios are stored as exact `a/b` fraction strings so they reproduce the legacy Java double bit-for-bit.
- **CANivore is named `"Persian  Canivore"` — the DOUBLE SPACE is the real device name.** Mechanisms are on a second bus, `"mechanisms"` (RobotConstants.kMechanismBus / the `bus` field in mechanisms.json).
- **Boot procedure is load-bearing:** turret physically straight forward and hood at bottom hard stop at power-on. Both are rotor-zeroed by `RealMechanism` (the `bootZero` flag in mechanisms.json); their CANcoders are configured but unfused (telemetry only). The intake pivot uses its fused remote CANcoder (no boot zero). Operator RB re-zeroes the turret mid-match (`Mechanisms.zero("turret")`).
- **Turret wrap** (`runtime.TurretWrap.apply`): setpoint past a soft limit (−0.73/+0.39 rot) gets ±1.0 full rotation then clamps; `TurretAiming` applies the wrap itself and stores the ACTUALLY-commanded value back, then the interpreter commands exactly that (turret has no further output clamp) — or its accumulator winds up. Single ±1 correction only; decision ignores current position. All deliberate, all tested.
- **Turret gyro feedforward ADDS Pigeon yaw deltas** (`runtime.TurretAiming.applyGyroFf`) — the sign encodes the turret/gyro convention and is load-bearing. Aiming is pure visual servoing (target = current + tx/360); there is no pose-based turret aiming.
- **Vision frame dedupe is exact-timestamp and shared between hub and passing reads** — a fresh frame without the wanted tag burns the timestamp. Legacy quirk, kept deliberately.
- **Tag→hub vectors carry a hand-tuned −0.2 m lateral fudge on all 16 entries** (VisionConstants.TAG_TO_HUB_CENTER) — team-confirmed correct; do not "fix". Hub-before-passing tag classification order is load-bearing (the ID sets overlap).
- **Passing yaw aims along the tag's surface NORMAL** (lob into the zone the tag faces), not at the tag.
- **Pose estimation is hard-gated behind `VisionConstants.kTransformsMeasured` (currently false)** — inert until ROBOT_TO_TURRET / TURRET_TO_CAMERA hold real measurements. A guessed camera-on-turret transform silently poisons odometry; the gate is deliberate. Ambiguity gate is 0.3.
- **The feed gate is `aimed && atSpeed && shooterCommandedNonZero`** (`ScoringSequencer` T5) — the nonZero term prevents a 0-RPS setpoint from reading "at speed" and feeding a dead flywheel (the old robot's worst bug). Hood readiness is deliberately NOT in the gate. The sequencer's one-loop-per-transition timing is load-bearing: it is the enumerated reflex latch that a purely stateless gate cannot reproduce (e.g. PASS_SPINNING_UP must precede PASSING by a loop, and the speed-only pass gate W14 relies on this).
- **CLEARING is the single atomic transition:** shot release stops the shooter and reverses all indexers FULL speed for 2.0 s (`kClearingSeconds`) to back balls away from the flywheel. SHOOT re-press, UNJAM, and manual all exit/preempt it instantly; IDLE/PASS wait.
- **UNJAMMING spins the shooter FORWARD flat-out** (400 = clamp ceiling) while reversing indexers at half speed — intended, ejects pinched balls.
- **Intake has the robot's only nested state machine** (deploy latch at 15°, stow-stops-rollers-first, over-travel recovery below 0.14 rot while stowing). `isOut()` means >5° off stow, NOT "deployed" — the idle vertical feed (26.25 RPS) keys off it. Over-travel recovery is active from boot and will fight a manual pivot jog below the threshold — that's protection, not a bug.
- **Manual mode = operator Start.** The interpreter commands nothing in manual except the intake axis; `LocalSkillDriver.applyManualControls` drives turret/hood/shooter/indexer directly each loop (no WPILib default commands — the runtime has no per-mechanism subsystems). Manual PERSISTS across disable (held in `SkillServer`); the scoring phase resets to IDLE on `onEnable()`. POV turret presets and X/Y/B hood presets work ONLY in manual mode.
- **Shoot-while-moving** lives in `ShotCalculator.effectiveDistance` (in `util/`, reused by `Setpoints`) behind NT toggle `Tuning/ShootWhileMovingEnabled`, force-set OFF at every boot.
- **Interp tables** (ShotCalculator) are calibrated field data (2026-03-17). The legacy `CalibrationCommand` was deleted at the skill-server cutover and NOT yet re-ported to the mechanism layer — recalibration needs a new command that drives `Mechanisms.setVelocity/setPosition` from the /Calibration NT entries.
- **Constants comments may lie; VALUES are law** (e.g. TURRET_POS_RIGHT 0.25 rot = 90°, not the commented 18°). Never round, convert, or "clean" a tuned number.
- PathPlanner loads robot config from `deploy/pathplanner/settings.json` (`RobotConfig.fromGUISettings`) — a second source of truth vs TunerConstants (5.44 vs 5.85 m/s max, known mismatch). The `.auto` files reference named commands by exact string: intakeOut, intakeIn, shoot, stopAll, autoShoot, feedIndexers, reverseIndexer. One path file has a LEADING SPACE in its name (" Manual Depot to Outpost") and is referenced with it — renaming breaks the auto.
- `MATCH_MODE` in Robot.java gates Epilogue NT verbosity (false = DEBUG+, true = CRITICAL only). HootAutoReplay runs unconditionally in robotPeriodic (deterministic log replay; no-op on a field).

## Learned gotchas

- Epilogue's annotation processor breaks on two `@Logged` classes with the same SIMPLE name in different packages (its generated binder single-type-imports both `FooLogger`s). That's why the legacy archive has no Epilogue annotations.
- JUnit tests touching any WPILib sim class must call `HAL.initialize(500, 0)` in `@BeforeAll` — sim classes read battery voltage through the HAL and SIGSEGV natively without it.
- Test classes that construct subsystems must `CommandScheduler.getInstance().unregisterAllSubsystems()` in setup AND teardown — SubsystemBase self-registers, and a leaked subsystem's periodic runs inside OTHER test classes' scheduler loops.
- `PhotonTrackedTarget`'s constructor asserts exactly 4 corners per corner list — synthetic test frames need real TargetCorner lists, not `List.of()`.
- Never re-add a second Phoenix6 vendordep json — duplicates share a UUID and Gradle's behavior is undefined (26.1.1 was deleted for this).
- The repo's working refactor docs (FUNCTIONALITY_INVENTORY/REFACTOR_DESIGN/REFACTOR_PROGRESS) are gitignored by team decision — they live on disk only. `docs/skills.md` (which supersedes the old `docs/superstructure.md`) is the committed source of truth — keep it current when a skill, reflex, setpoint source, or phase transition changes (non-negotiable #8).
- The skill-server runtime has no per-mechanism `@Logged` subsystems, but two `@Logged` classes with the same SIMPLE name in different packages still break Epilogue (that's why `runtime.TurretAiming` is NOT `@Logged` — `legacy` had its own). Keep new `@Logged` runtime class names unique.
- The setpoint-equivalence harness (`EquivalenceHarnessTest`, deleted at cutover with the legacy stack) proved per-tick identical mechanism commands across the W# grid. Its successors are `SkillInterpreterFlowTest` (flows) + `ScoringSequencerTest` (pure transitions) — the permanent behavior catalog.
